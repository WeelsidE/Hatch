//Это функционал споллера (Не полная а на этапе открыть закрыть)
/* const spolers = document.querySelectorAll('.click');

if (spolers) {
    for (let i = 0;i < spolers.length; i++) {
        const spoler = spolers[i];

        spoler.addEventListener('click', function (e) {
            let href = spoler.getAttribute('href').replace('#', '');
        
            const curentModal = document.getElementById(href);

            curentModal.classList.toggle('_active');
            
            e.preventDefault();
        });
    }
} */